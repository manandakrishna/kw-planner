import React from 'react';
import { Typography } from '@mui/material';

export default function BlogsPage() {
  return (
    <div>
      <Typography variant="h4">Blogs Page</Typography>
    </div>
  );
}